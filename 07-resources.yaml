kind: Pod
apiversion: v1
metadata:
  name: env
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "68Mi"
        cpu: "100m"
      limits:
        memory: "129Mi"
        cpu: "150m"